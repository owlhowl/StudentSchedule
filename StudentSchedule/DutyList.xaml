<Page x:Class="StudentSchedule.DutyList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentSchedule"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" d:Background="White"
      Title="DutyList">

    <StackPanel VerticalAlignment="Center">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>
            <TextBlock Text="Первый дежурный"/>
            <TextBox Text="{Binding FirstStudent}" Padding="3" Margin="0 0 5 5" Width="250"/>
        </StackPanel>
        <StackPanel>
            <TextBlock Text="Второй дежурный"/>
            <TextBox Text="{Binding SecondStudent}" Padding="3" Margin=" 0 0 5 5" Width="250"/>
        </StackPanel>
    </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Command="{Binding Agree}" Content="Подтвердить"/>
            <Button Command="{Binding BackToList}"  Content="Назад"/>
        </StackPanel>
    </StackPanel>
</Page>
